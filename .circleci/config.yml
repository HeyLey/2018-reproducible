version: 2
jobs: 
  build:
    machine: 
        python:
            version: 3.5.2 
    working_directory: ~/knn 
    steps:
      - checkout 
      - run: 
          name: install dependencies
          command: |
            pyenv versions 
            python3 -m venv venv
            . venv/bin/activate
            pip install -r requirements.txt
